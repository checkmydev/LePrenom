# LePrenom Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire une PWA-jeu (vanilla JS) pour aider à choisir un prénom : notation à l'aveugle en étoiles, analyse IA des favoris, duels ELO, dashboard Top 10 et accord Maman/Papa, avec stockage Supabase.

**Architecture :** Single-page app statique (HTML/CSS/JS vanilla, écrans en sections affichées/masquées par un mini-routeur). Catalogue de prénoms généré au build depuis l'INSEE et embarqué (`data/prenoms.json`). Données de jeu persistées dans Supabase (schéma `leprenom`) via la clé `anon`. Analyse IA via une Supabase Edge Function appelant Claude. Déploiement GitHub Pages via GitHub Actions.

**Tech Stack :** HTML5, CSS3, JavaScript ES modules (navigateur), Supabase (`@supabase/supabase-js` via CDN ESM), Deno (Edge Function), Node.js ≥ 18 (scripts de build + tests avec `node:test`), GitHub Actions.

---

## Constantes partagées (référencées par plusieurs tâches)

```
SUPABASE_URL   = https://wzrcrszfubjsfoaxatvo.supabase.co
SUPABASE_ANON  = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind6cmNyc3pmdWJqc2ZvYXhhdHZvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzYxMjcxMzEsImV4cCI6MjA5MTcwMzEzMX0.HSzERXdF0nBs0L4XVcKo-UsGe1PAcqiVD5gwZj15foY
NOM_FAMILLE    = "Gerard"
SEUIL_ANALYSE_IA = 7          # note strictement > 7 déclenche l'IA
SEUIL_COUP_DE_COEUR = 7       # note >= 7 par les DEUX parents
ELO_K = 32
ELO_DEFAUT = 1000
MANCHE_TAILLE = 10            # prénoms par manche
```

> La clé `anon` est publique par conception (OK dans le repo). Ne JAMAIS committer `service_role` ni `ANTHROPIC_API_KEY`.

---

## File Structure

- `index.html` — shell SPA : header/nav + une `<section>` par écran (accueil, jeu, duel, dashboard, favoris, fiche).
- `css/styles.css` — styles + animations (décompte, étoiles).
- `js/config.js` — constantes ci-dessus (URL, clé anon, seuils).
- `js/supabase.js` — init client + toutes les requêtes DB (ratings/favoris/elo/analyses).
- `js/catalog.js` — chargement `data/prenoms.json`, tirage aléatoire d'une manche, tirage d'une paire de duel.
- `js/stars.js` — composant notation étoiles réutilisable (1→10, rendu + événement).
- `js/profile.js` — gestion profil courant (Maman/Papa) via localStorage.
- `js/game.js` — écran Jeu : décompte 3-2-1-GO, manche de 10, notation, déclenchement IA.
- `js/duel.js` — écran Duel + calcul ELO (fonction pure `computeElo`).
- `js/dashboard.js` — Top 10 + onglet accord parents.
- `js/favoris.js` — short-list.
- `js/fiche.js` — fiche prénom + affichage analyse IA.
- `js/ia.js` — appel Edge Function `analyser-prenom`.
- `js/app.js` — routeur (hash), init, câblage de la nav.
- `data/prenoms.json` — catalogue généré (commité).
- `scripts/build-prenoms.mjs` — télécharge le fichier INSEE, parse, nettoie → `data/prenoms.json`.
- `scripts/insee-parse.mjs` — fonctions pures de parsing/nettoyage INSEE (testables).
- `test/insee-parse.test.mjs`, `test/catalog.test.mjs`, `test/elo.test.mjs` — tests `node:test`.
- `supabase/schema.sql` — schéma `leprenom` + tables + RLS.
- `supabase/functions/analyser-prenom/index.ts` — Edge Function Claude.
- `manifest.json`, `sw.js`, `icons/` — PWA.
- `.github/workflows/deploy.yml` — déploiement Pages via Actions.
- `package.json` — scripts npm (`build:prenoms`, `test`, `serve`).

---

## Task 1: Scaffold projet + package.json + serveur local

**Files:**
- Create: `package.json`
- Create: `index.html`
- Create: `css/styles.css`
- Create: `js/app.js`
- Create: `js/config.js`

- [ ] **Step 1: Créer `package.json`**

```json
{
  "name": "leprenom",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:prenoms": "node scripts/build-prenoms.mjs",
    "test": "node --test",
    "serve": "npx --yes serve@14 -l 5000 ."
  }
}
```

- [ ] **Step 2: Créer `js/config.js`**

```js
export const SUPABASE_URL = "https://wzrcrszfubjsfoaxatvo.supabase.co";
export const SUPABASE_ANON = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind6cmNyc3pmdWJqc2ZvYXhhdHZvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzYxMjcxMzEsImV4cCI6MjA5MTcwMzEzMX0.HSzERXdF0nBs0L4XVcKo-UsGe1PAcqiVD5gwZj15foY";
export const NOM_FAMILLE = "Gerard";
export const SEUIL_ANALYSE_IA = 7;       // note > 7
export const SEUIL_COUP_DE_COEUR = 7;    // note >= 7 par les deux parents
export const MANCHE_TAILLE = 10;
export const ELO_K = 32;
export const ELO_DEFAUT = 1000;
```

- [ ] **Step 3: Créer `index.html` (shell SPA)**

```html
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#7c3aed" />
  <link rel="manifest" href="manifest.json" />
  <link rel="stylesheet" href="css/styles.css" />
  <title>LePrenom</title>
</head>
<body>
  <header id="topbar">
    <span id="brand">👶 LePrenom</span>
    <span id="who"></span>
  </header>

  <main>
    <section id="screen-accueil" class="screen"></section>
    <section id="screen-jeu" class="screen hidden"></section>
    <section id="screen-duel" class="screen hidden"></section>
    <section id="screen-dashboard" class="screen hidden"></section>
    <section id="screen-favoris" class="screen hidden"></section>
    <section id="screen-fiche" class="screen hidden"></section>
  </main>

  <nav id="tabbar" class="hidden">
    <a href="#jeu">🎮 Jeu</a>
    <a href="#duel">⚔️ Duel</a>
    <a href="#dashboard">🏆 Top</a>
    <a href="#favoris">❤️ Favoris</a>
  </nav>

  <script type="module" src="js/app.js"></script>
</body>
</html>
```

- [ ] **Step 4: Créer `css/styles.css` (base — sera enrichi plus tard)**

```css
:root { --violet:#7c3aed; --violet2:#a78bfa; --bg:#faf7ff; --card:#fff; --ink:#1f2937; --or:#fbbf24; }
* { box-sizing: border-box; }
body { margin:0; font-family: system-ui, sans-serif; background:var(--bg); color:var(--ink); }
#topbar { display:flex; justify-content:space-between; align-items:center; padding:12px 16px; background:var(--violet); color:#fff; position:sticky; top:0; }
#brand { font-weight:700; }
main { padding:16px; max-width:720px; margin:0 auto 80px; }
.screen.hidden, .hidden { display:none; }
.btn { display:inline-flex; align-items:center; gap:8px; border:0; border-radius:14px; padding:14px 22px; font-size:1rem; font-weight:600; background:var(--violet); color:#fff; cursor:pointer; }
.btn.secondary { background:var(--violet2); }
.card { background:var(--card); border-radius:16px; padding:16px; margin:12px 0; box-shadow:0 2px 10px rgba(124,58,237,.08); }
#tabbar { position:fixed; bottom:0; left:0; right:0; display:flex; justify-content:space-around; background:#fff; border-top:1px solid #eee; padding:8px 0; }
#tabbar a { text-decoration:none; color:var(--ink); font-size:.85rem; padding:6px 10px; border-radius:10px; }
#tabbar a.active { background:var(--bg); color:var(--violet); font-weight:700; }
```

- [ ] **Step 5: Créer `js/app.js` (routeur minimal — écrans branchés dans les tâches suivantes)**

```js
const screens = ["accueil","jeu","duel","dashboard","favoris","fiche"];

export function show(name) {
  for (const s of screens) {
    document.getElementById(`screen-${s}`).classList.toggle("hidden", s !== name);
  }
  document.querySelectorAll("#tabbar a").forEach(a =>
    a.classList.toggle("active", a.getAttribute("href") === `#${name}`));
}

function route() {
  const name = (location.hash.replace("#","") || "accueil");
  show(screens.includes(name) ? name : "accueil");
}

window.addEventListener("hashchange", route);
window.addEventListener("DOMContentLoaded", () => {
  document.getElementById("screen-accueil").innerHTML =
    `<div class="card"><h1>👶 LePrenom</h1><p>Scaffold OK — écrans branchés dans les tâches suivantes.</p></div>`;
  route();
});
```

- [ ] **Step 6: Vérifier dans le navigateur**

Run: `npm run serve`
Ouvrir http://localhost:5000 → page "Scaffold OK" visible, pas d'erreur console.

- [ ] **Step 7: Commit**

```bash
git add package.json index.html css/styles.css js/app.js js/config.js
git commit -m "feat: scaffold SPA + routeur + config"
```

---

## Task 2: Parsing INSEE (fonctions pures, TDD)

Le fichier INSEE `nat2022.csv` (ou version courante) a des colonnes `sexe;preusuel;annais;nombre` séparées par `;`. `sexe` = 1 (garçon) / 2 (fille). On agrège le total par prénom, on filtre les `_PRENOMS_RARES` et `preusuel` invalides, on garde un `count` total par (prénom, sexe).

**Files:**
- Create: `scripts/insee-parse.mjs`
- Test: `test/insee-parse.test.mjs`

- [ ] **Step 1: Écrire le test qui échoue**

```js
// test/insee-parse.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { parseInsee, normalizePrenom } from "../scripts/insee-parse.mjs";

test("normalizePrenom met en Capitale et trim", () => {
  assert.equal(normalizePrenom("  LÉA "), "Léa");
  assert.equal(normalizePrenom("JEAN-PIERRE"), "Jean-Pierre");
});

test("parseInsee agrège par prénom+sexe et ignore les rares/invalides", () => {
  const csv = [
    "sexe;preusuel;annais;nombre",
    "2;LEA;2000;100",
    "2;LEA;2001;50",
    "1;GABRIEL;2001;200",
    "1;_PRENOMS_RARES;2001;9999",
    "2;X;2001;5",
  ].join("\n");
  const out = parseInsee(csv);
  const lea = out.find(p => p.prenom === "Lea" && p.sexe === "f");
  assert.equal(lea.count, 150);
  assert.ok(out.find(p => p.prenom === "Gabriel" && p.sexe === "m"));
  assert.ok(!out.find(p => p.prenom.includes("RARES")));
  assert.ok(!out.find(p => p.prenom === "X"), "prénom d'une lettre exclu");
});
```

- [ ] **Step 2: Lancer le test → échec attendu**

Run: `node --test test/insee-parse.test.mjs`
Expected: FAIL (`Cannot find module ../scripts/insee-parse.mjs`).

- [ ] **Step 3: Implémenter `scripts/insee-parse.mjs`**

```js
// Fonctions pures de parsing/nettoyage du fichier INSEE des prénoms.

export function normalizePrenom(raw) {
  const s = String(raw).trim().toLowerCase();
  // Capitale sur chaque segment séparé par - ou espace
  return s.replace(/(^|[-\s])([a-zàâäéèêëîïôöùûüç])/g,
    (_, sep, ch) => sep + ch.toUpperCase());
}

export function parseInsee(csvText) {
  const lines = csvText.split(/\r?\n/).filter(Boolean);
  const map = new Map(); // key `${prenom}|${sexe}` -> count
  for (let i = 1; i < lines.length; i++) {
    const [sexe, preusuel, , nombre] = lines[i].split(";");
    if (!preusuel || preusuel.startsWith("_")) continue;        // _PRENOMS_RARES
    if (preusuel.replace(/[-\s]/g, "").length < 2) continue;    // 1 lettre / vide
    const prenom = normalizePrenom(preusuel);
    const sx = sexe === "1" ? "m" : sexe === "2" ? "f" : null;
    if (!sx) continue;
    const key = `${prenom}|${sx}`;
    const n = parseInt(nombre, 10) || 0;
    map.set(key, (map.get(key) || 0) + n);
  }
  return [...map.entries()].map(([key, count]) => {
    const [prenom, sexe] = key.split("|");
    return { prenom, sexe, count };
  });
}
```

- [ ] **Step 4: Lancer le test → succès**

Run: `node --test test/insee-parse.test.mjs`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add scripts/insee-parse.mjs test/insee-parse.test.mjs
git commit -m "feat: parsing INSEE prénoms (fonctions pures + tests)"
```

---

## Task 3: Script de build du catalogue `prenoms.json`

Télécharge le CSV INSEE, applique `parseInsee`, filtre les prénoms trop rares (`count < 200` pour garder une base gérable ~3-5k prénoms), trie, écrit `data/prenoms.json`.

Source INSEE (fichier « Fichier des prénoms » niveau national). URL de repli configurable via variable d'env `INSEE_URL`. Si le téléchargement échoue, le script lit un fichier local `scripts/nat.csv` si présent.

**Files:**
- Create: `scripts/build-prenoms.mjs`
- Create: `data/prenoms.json` (généré)

- [ ] **Step 1: Implémenter `scripts/build-prenoms.mjs`**

```js
import { writeFileSync, readFileSync, existsSync, mkdirSync } from "node:fs";
import { parseInsee } from "./insee-parse.mjs";

const INSEE_URL = process.env.INSEE_URL
  || "https://www.insee.fr/fr/statistiques/fichier/8205621/prenoms-2023-csv.zip"; // à adapter si le lien évolue
const MIN_COUNT = Number(process.env.MIN_COUNT || 200);
const LOCAL = "scripts/nat.csv";

async function getCsv() {
  if (existsSync(LOCAL)) {
    console.log("Lecture locale", LOCAL);
    return readFileSync(LOCAL, "utf8");
  }
  console.log("Téléchargement", INSEE_URL);
  const res = await fetch(INSEE_URL);
  if (!res.ok) throw new Error(`HTTP ${res.status} — placez le CSV décompressé dans ${LOCAL}`);
  const ct = res.headers.get("content-type") || "";
  if (ct.includes("zip") || INSEE_URL.endsWith(".zip")) {
    throw new Error(`Fichier ZIP: décompressez et placez le .csv dans ${LOCAL}, puis relancez.`);
  }
  return await res.text();
}

const csv = await getCsv();
let list = parseInsee(csv).filter(p => p.count >= MIN_COUNT);
list.sort((a, b) => b.count - a.count);
mkdirSync("data", { recursive: true });
writeFileSync("data/prenoms.json", JSON.stringify(list));
console.log(`Écrit data/prenoms.json — ${list.length} prénoms (min count=${MIN_COUNT}).`);
```

- [ ] **Step 2: Générer le catalogue**

Run: `npm run build:prenoms`
Si l'URL INSEE renvoie un ZIP ou 404 : télécharger manuellement le CSV national des prénoms depuis insee.fr, le décompresser, le placer dans `scripts/nat.csv`, puis relancer `npm run build:prenoms`.
Expected: `data/prenoms.json` créé, message « Écrit data/prenoms.json — N prénoms » (N ≈ 3000–6000).

- [ ] **Step 3: Vérification rapide du contenu**

Run: `node -e "const a=require('./data/prenoms.json'); console.log(a.length, a[0], a.some(p=>p.sexe==='f'), a.some(p=>p.sexe==='m'))"`
Expected: nombre > 1000, présence de `f` et `m`, chaque item = `{prenom, sexe, count}`.

- [ ] **Step 4: Commit (catalogue commité pour un build Pages sans dépendance réseau)**

```bash
git add scripts/build-prenoms.mjs data/prenoms.json
git commit -m "feat: build catalogue prénoms INSEE -> data/prenoms.json"
```

---

## Task 4: Catalogue côté client — chargement + tirages (TDD sur la logique pure)

**Files:**
- Create: `js/catalog.js`
- Test: `test/catalog.test.mjs`

- [ ] **Step 1: Écrire le test qui échoue (logique pure de tirage)**

```js
// test/catalog.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { pickRound, pickPair } from "../js/catalog.js";

const data = Array.from({ length: 50 }, (_, i) => ({
  prenom: `P${i}`, sexe: i % 2 ? "f" : "m", count: 100 + i,
}));

test("pickRound retourne N prénoms distincts", () => {
  const r = pickRound(data, 10, () => 0.42);
  assert.equal(r.length, 10);
  assert.equal(new Set(r.map(p => p.prenom)).size, 10);
});

test("pickRound plafonne à la taille dispo", () => {
  assert.equal(pickRound(data.slice(0, 3), 10).length, 3);
});

test("pickPair retourne 2 prénoms distincts", () => {
  const [a, b] = pickPair(data);
  assert.notEqual(a.prenom, b.prenom);
});
```

- [ ] **Step 2: Lancer → échec**

Run: `node --test test/catalog.test.mjs`
Expected: FAIL (module introuvable).

- [ ] **Step 3: Implémenter `js/catalog.js`**

```js
let _cache = null;

export async function loadCatalog() {
  if (_cache) return _cache;
  const res = await fetch("data/prenoms.json");
  _cache = await res.json();
  return _cache;
}

// Fisher-Yates partiel ; rnd injectable pour les tests
export function pickRound(list, n = 10, rnd = Math.random) {
  const arr = list.slice();
  const k = Math.min(n, arr.length);
  for (let i = 0; i < k; i++) {
    const j = i + Math.floor(rnd() * (arr.length - i));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr.slice(0, k);
}

export function pickPair(list, rnd = Math.random) {
  const [a, b] = pickRound(list, 2, rnd);
  return [a, b];
}
```

- [ ] **Step 4: Lancer → succès**

Run: `node --test test/catalog.test.mjs`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add js/catalog.js test/catalog.test.mjs
git commit -m "feat: catalogue client + tirage manche/paire (TDD)"
```

---

## Task 5: Client Supabase (requêtes DB)

Utilise `@supabase/supabase-js` en ESM depuis un CDN. Toutes les requêtes DB centralisées ici. Schéma `leprenom` ciblé via `.schema("leprenom")`.

**Files:**
- Create: `js/supabase.js`

- [ ] **Step 1: Implémenter `js/supabase.js`**

```js
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { SUPABASE_URL, SUPABASE_ANON } from "./config.js";

const sb = createClient(SUPABASE_URL, SUPABASE_ANON, {
  db: { schema: "leprenom" },
});

// --- Ratings ---
export async function upsertRating({ prenom, sexe, parent, note }) {
  const { error } = await sb.from("ratings")
    .upsert({ prenom, sexe, parent, note, updated_at: new Date().toISOString() },
            { onConflict: "prenom,parent" });
  if (error) throw error;
}

export async function fetchRatings() {
  const { data, error } = await sb.from("ratings").select("*");
  if (error) throw error;
  return data;
}

// --- Favoris ---
export async function toggleFavori(prenom, parent, on) {
  if (on) {
    const { error } = await sb.from("favoris")
      .upsert({ prenom, parent }, { onConflict: "prenom,parent" });
    if (error) throw error;
  } else {
    const { error } = await sb.from("favoris").delete().match({ prenom, parent });
    if (error) throw error;
  }
}

export async function fetchFavoris() {
  const { data, error } = await sb.from("favoris").select("*");
  if (error) throw error;
  return data;
}

// --- ELO / duels ---
export async function fetchElo() {
  const { data, error } = await sb.from("elo").select("*");
  if (error) throw error;
  return data;
}

export async function saveElo(rows) {
  const { error } = await sb.from("elo").upsert(rows, { onConflict: "prenom" });
  if (error) throw error;
}

export async function recordDuel({ gagnant, perdant, parent }) {
  const { error } = await sb.from("duel_results").insert({ gagnant, perdant, parent });
  if (error) throw error;
}

// --- Analyses (cache IA) ---
export async function fetchAnalyse(prenom) {
  const { data, error } = await sb.from("analyses").select("*").eq("prenom", prenom).maybeSingle();
  if (error) throw error;
  return data;
}
```

- [ ] **Step 2: Vérification de syntaxe (import résolu au runtime navigateur)**

Run: `node -e "require('fs').readFileSync('js/supabase.js','utf8').includes('createClient') && console.log('ok')"`
Expected: `ok`. (Test fonctionnel réel après Task 6 : schéma créé.)

- [ ] **Step 3: Commit**

```bash
git add js/supabase.js
git commit -m "feat: client Supabase + requêtes ratings/favoris/elo/analyses"
```

---

## Task 6: Schéma Supabase (SQL + RLS) et application

**Files:**
- Create: `supabase/schema.sql`

- [ ] **Step 1: Écrire `supabase/schema.sql`**

```sql
create schema if not exists leprenom;

create table if not exists leprenom.ratings (
  id bigint generated always as identity primary key,
  prenom text not null,
  sexe text check (sexe in ('f','m')),
  parent text not null check (parent in ('maman','papa')),
  note int not null check (note between 1 and 10),
  updated_at timestamptz not null default now(),
  unique (prenom, parent)
);

create table if not exists leprenom.favoris (
  id bigint generated always as identity primary key,
  prenom text not null,
  parent text not null check (parent in ('maman','papa')),
  created_at timestamptz not null default now(),
  unique (prenom, parent)
);

create table if not exists leprenom.elo (
  prenom text primary key,
  score int not null default 1000,
  matches int not null default 0
);

create table if not exists leprenom.duel_results (
  id bigint generated always as identity primary key,
  gagnant text not null,
  perdant text not null,
  parent text not null check (parent in ('maman','papa')),
  created_at timestamptz not null default now()
);

create table if not exists leprenom.analyses (
  prenom text primary key,
  sexe text,
  signification text,
  description text,
  jeux_de_mots text,
  compat_gerard text,
  raw jsonb,
  created_at timestamptz not null default now()
);

-- Expose le schéma à l'API REST (à faire aussi dans Dashboard > API > Exposed schemas)
grant usage on schema leprenom to anon, authenticated;
grant all on all tables in schema leprenom to anon, authenticated;
grant all on all sequences in schema leprenom to anon, authenticated;
alter default privileges in schema leprenom grant all on tables to anon, authenticated;
alter default privileges in schema leprenom grant all on sequences to anon, authenticated;

-- RLS ouvertes (usage familial assumé)
alter table leprenom.ratings enable row level security;
alter table leprenom.favoris enable row level security;
alter table leprenom.elo enable row level security;
alter table leprenom.duel_results enable row level security;
alter table leprenom.analyses enable row level security;

do $$
declare t text;
begin
  for t in select unnest(array['ratings','favoris','elo','duel_results','analyses']) loop
    execute format('drop policy if exists open_all on leprenom.%I;', t);
    execute format('create policy open_all on leprenom.%I for all to anon, authenticated using (true) with check (true);', t);
  end loop;
end $$;
```

- [ ] **Step 2: Appliquer le schéma dans Supabase**

Dans le Dashboard Supabase du projet `wzrcrszfubjsfoaxatvo` → **SQL Editor** → coller le contenu de `supabase/schema.sql` → **Run**.
Puis **Settings → API → Exposed schemas** : ajouter `leprenom` et sauvegarder.
Expected: exécution sans erreur, 5 tables visibles dans `leprenom`.

- [ ] **Step 3: Test fonctionnel du client (via la page servie)**

Run: `npm run serve`, ouvrir http://localhost:5000, dans la console navigateur :
```js
const m = await import('./js/supabase.js');
await m.upsertRating({prenom:'Test', sexe:'f', parent:'maman', note:5});
console.log(await m.fetchRatings());
```
Expected: aucun `error`, la ligne `Test` apparaît. (Nettoyer ensuite : la supprimer via SQL Editor.)

- [ ] **Step 4: Commit**

```bash
git add supabase/schema.sql
git commit -m "feat: schéma Supabase leprenom + RLS ouvertes"
```

---

## Task 7: Profil (Maman/Papa) + écran Accueil

**Files:**
- Create: `js/profile.js`
- Modify: `js/app.js` (rendu accueil + affichage profil + garde de nav)

- [ ] **Step 1: Créer `js/profile.js`**

```js
const KEY = "leprenom.parent";

export function getParent() {
  const v = localStorage.getItem(KEY);
  return v === "maman" || v === "papa" ? v : null;
}

export function setParent(p) {
  localStorage.setItem(KEY, p);
}

export function labelParent(p) {
  return p === "maman" ? "👩 Maman" : p === "papa" ? "👨 Papa" : "";
}
```

- [ ] **Step 2: Modifier `js/app.js` — rendu accueil + garde**

Remplacer le handler `DOMContentLoaded` par :

```js
import { getParent, setParent, labelParent } from "./profile.js";

function renderAccueil() {
  const p = getParent();
  const el = document.getElementById("screen-accueil");
  el.innerHTML = `
    <div class="card" style="text-align:center">
      <h1>👶 LePrenom</h1>
      <p>Qui joue ?</p>
      <div style="display:flex; gap:12px; justify-content:center; flex-wrap:wrap">
        <button class="btn" data-p="maman">👩 Maman</button>
        <button class="btn secondary" data-p="papa">👨 Papa</button>
      </div>
      ${p ? `<p style="margin-top:16px">Profil actuel : <b>${labelParent(p)}</b><br>
        <a href="#jeu">Commencer à jouer →</a></p>` : ""}
    </div>`;
  el.querySelectorAll("button[data-p]").forEach(b =>
    b.addEventListener("click", () => { setParent(b.dataset.p); location.hash = "#jeu"; refreshWho(); renderAccueil(); }));
}

function refreshWho() {
  const p = getParent();
  document.getElementById("who").textContent = p ? labelParent(p) : "";
  document.getElementById("tabbar").classList.toggle("hidden", !p);
}

window.addEventListener("DOMContentLoaded", () => {
  renderAccueil();
  refreshWho();
  route();
});
```

Ajouter en tête de `route()` une garde : si aucun parent choisi et cible ≠ accueil, forcer l'accueil :

```js
function route() {
  const p = getParent();
  let name = (location.hash.replace("#","") || "accueil");
  if (!p && name !== "accueil") name = "accueil";
  show(screens.includes(name) ? name : "accueil");
}
```

- [ ] **Step 3: Vérifier dans le navigateur**

Run: `npm run serve` → cliquer Maman → header affiche "👩 Maman", tabbar apparaît, on arrive sur l'écran Jeu (vide pour l'instant).
Recharger sans profil (effacer localStorage) → toute nav renvoie à l'accueil.

- [ ] **Step 4: Commit**

```bash
git add js/profile.js js/app.js
git commit -m "feat: profil Maman/Papa + écran accueil + garde de nav"
```

---

## Task 8: Composant étoiles (notation 1→10)

10 étoiles cliquables ; survol/clic remplit jusqu'à l'index ; émet la note choisie.

**Files:**
- Create: `js/stars.js`
- Modify: `css/styles.css` (styles étoiles)

- [ ] **Step 1: Créer `js/stars.js`**

```js
// Rend 10 étoiles dans `container`. onRate(note 1..10) appelé au clic.
export function renderStars(container, { value = 0, onRate } = {}) {
  container.classList.add("stars");
  container.innerHTML = "";
  const paint = (n) => [...container.children].forEach((s, i) =>
    s.classList.toggle("on", i < n));
  for (let i = 1; i <= 10; i++) {
    const s = document.createElement("span");
    s.className = "star";
    s.textContent = "★";
    s.addEventListener("mouseenter", () => paint(i));
    s.addEventListener("click", () => { paint(i); onRate && onRate(i); });
    container.appendChild(s);
  }
  container.addEventListener("mouseleave", () => paint(value));
  paint(value);
}
```

- [ ] **Step 2: Ajouter les styles dans `css/styles.css`**

```css
.stars { display:inline-flex; gap:2px; cursor:pointer; font-size:1.5rem; }
.star { color:#e5e7eb; transition:color .08s; }
.star.on { color:var(--or); }
.countdown { position:fixed; inset:0; display:flex; align-items:center; justify-content:center; font-size:6rem; font-weight:800; color:var(--violet); background:rgba(255,255,255,.92); z-index:50; }
@keyframes pop { 0%{transform:scale(.4);opacity:0} 60%{transform:scale(1.15);opacity:1} 100%{transform:scale(1)} }
.countdown span { animation:pop .8s ease; }
.prenom-nom { font-size:1.4rem; font-weight:700; }
.badge-sexe { font-size:.8rem; padding:2px 8px; border-radius:999px; }
.badge-sexe.f { background:#fce7f3; color:#be185d; }
.badge-sexe.m { background:#dbeafe; color:#1d4ed8; }
```

- [ ] **Step 3: Vérifier isolément**

Run: `npm run serve`, console navigateur :
```js
const {renderStars} = await import('./js/stars.js');
renderStars(document.getElementById('screen-jeu'), {onRate:n=>console.log('note',n)});
```
Expected: 10 étoiles, survol/clic loggue la note.

- [ ] **Step 4: Commit**

```bash
git add js/stars.js css/styles.css
git commit -m "feat: composant étoiles 1->10 + styles jeu"
```

---

## Task 9: Écran Jeu — décompte 3-2-1-GO + manche de 10 + notation

**Files:**
- Create: `js/game.js`
- Modify: `js/app.js` (init du jeu à l'affichage de l'écran)

- [ ] **Step 1: Créer `js/game.js`**

```js
import { loadCatalog, pickRound } from "./catalog.js";
import { renderStars } from "./stars.js";
import { getParent } from "./profile.js";
import { upsertRating, toggleFavori } from "./supabase.js";
import { MANCHE_TAILLE, SEUIL_ANALYSE_IA } from "./config.js";
import { analyserPrenom } from "./ia.js";

const el = () => document.getElementById("screen-jeu");

function countdown() {
  return new Promise((resolve) => {
    const layer = document.createElement("div");
    layer.className = "countdown";
    document.body.appendChild(layer);
    const seq = ["3","2","1","GO!"];
    let i = 0;
    const tick = () => {
      layer.innerHTML = `<span>${seq[i]}</span>`;
      i++;
      if (i < seq.length) setTimeout(tick, 800);
      else setTimeout(() => { layer.remove(); resolve(); }, 700);
    };
    tick();
  });
}

async function startRound() {
  await countdown();
  const cat = await loadCatalog();
  const round = pickRound(cat, MANCHE_TAILLE);
  const parent = getParent();
  el().innerHTML = `<h2>Note ces prénoms</h2><div id="round"></div>
    <button class="btn" id="rejouer">🔄 Nouvelle manche</button>`;
  const wrap = el().querySelector("#round");
  for (const p of round) {
    const card = document.createElement("div");
    card.className = "card";
    card.innerHTML = `
      <div style="display:flex; justify-content:space-between; align-items:center">
        <span class="prenom-nom">${p.prenom}</span>
        <span class="badge-sexe ${p.sexe}">${p.sexe === "f" ? "fille" : "garçon"}</span>
      </div>
      <div class="rate"></div>
      <div class="ia-slot" style="margin-top:8px; color:#6b21a8; font-size:.9rem"></div>`;
    const slot = card.querySelector(".ia-slot");
    renderStars(card.querySelector(".rate"), {
      onRate: async (note) => {
        try {
          await upsertRating({ prenom: p.prenom, sexe: p.sexe, parent, note });
          slot.textContent = "✔️ Noté " + note + "/10";
          if (note > SEUIL_ANALYSE_IA) {
            slot.textContent = "✨ Analyse du prénom en cours…";
            const a = await analyserPrenom(p.prenom, p.sexe);
            slot.innerHTML = `✨ <b>${p.prenom}</b> : ${a.signification || ""}
              <a href="#fiche" data-prenom="${p.prenom}">voir la fiche →</a>`;
          }
        } catch (e) {
          slot.textContent = "⚠️ Erreur : " + e.message;
        }
      },
    });
    wrap.appendChild(card);
  }
  el().querySelector("#rejouer").addEventListener("click", startRound);
}

export function initJeu() {
  const parent = getParent();
  if (!parent) { location.hash = "#accueil"; return; }
  el().innerHTML = `<div class="card" style="text-align:center">
      <h2>Prêt·e ?</h2><button class="btn" id="go">▶️ GO</button></div>`;
  el().querySelector("#go").addEventListener("click", startRound);
}
```

- [ ] **Step 2: Câbler l'init dans `js/app.js`**

Importer et appeler `initJeu()` quand on affiche le jeu. Modifier `show()` pour déclencher les inits :

```js
import { initJeu } from "./game.js";

const inits = { jeu: initJeu };
export function show(name) {
  for (const s of screens) {
    document.getElementById(`screen-${s}`).classList.toggle("hidden", s !== name);
  }
  document.querySelectorAll("#tabbar a").forEach(a =>
    a.classList.toggle("active", a.getAttribute("href") === `#${name}`));
  if (inits[name]) inits[name]();
}
```

- [ ] **Step 3: Vérifier dans le navigateur**

Run: `npm run serve` → profil Maman → onglet Jeu → GO → décompte 3-2-1-GO! → 10 cartes prénoms → cliquer des étoiles → « ✔️ Noté ». (L'analyse IA >7 sera opérationnelle après Task 11 ; ici elle peut afficher une erreur réseau, c'est attendu.)

- [ ] **Step 4: Commit**

```bash
git add js/game.js js/app.js
git commit -m "feat: écran Jeu (décompte 3-2-1-GO + manche de 10 + notation)"
```

---

## Task 10: Edge Function `analyser-prenom` (Claude)

**Files:**
- Create: `supabase/functions/analyser-prenom/index.ts`

- [ ] **Step 1: Écrire `supabase/functions/analyser-prenom/index.ts`**

```ts
import { createClient } from "jsr:@supabase/supabase-js@2";

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") return new Response("ok", { headers: CORS });
  try {
    const { prenom, sexe } = await req.json();
    if (!prenom) return json({ error: "prenom requis" }, 400);

    const sb = createClient(
      Deno.env.get("SUPABASE_URL")!,
      Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,
      { db: { schema: "leprenom" } },
    );

    // Cache
    const { data: cached } = await sb.from("analyses").select("*").eq("prenom", prenom).maybeSingle();
    if (cached) return json(cached);

    const prompt = `Tu es expert en prénoms francophones. Analyse le prénom "${prenom}" `
      + `(${sexe === "f" ? "fille" : "garçon"}) associé au nom de famille "Gerard".\n`
      + `Réponds STRICTEMENT en JSON avec les clés: signification (origine et sens, 1-2 phrases), `
      + `description (personnalité/perception, 1-2 phrases), jeux_de_mots (jeux de mots ou associations `
      + `possibles avec le prénom et/ou "Gerard", ou "aucun notable"), compat_gerard (verdict sur "PRENOM Gerard": `
      + `sonorité, initiales, fluidité — dis clairement si ça sonne bien ou si ça coince, 2-3 phrases).`;

    const aiRes = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "content-type": "application/json",
        "x-api-key": Deno.env.get("ANTHROPIC_API_KEY")!,
        "anthropic-version": "2023-06-01",
      },
      body: JSON.stringify({
        model: "claude-sonnet-5",
        max_tokens: 700,
        messages: [{ role: "user", content: prompt }],
      }),
    });
    const ai = await aiRes.json();
    const text = ai?.content?.[0]?.text ?? "{}";
    let parsed;
    try { parsed = JSON.parse(text.match(/\{[\s\S]*\}/)?.[0] ?? text); }
    catch { parsed = { signification: text, description: "", jeux_de_mots: "", compat_gerard: "" }; }

    const row = {
      prenom, sexe: sexe ?? null,
      signification: parsed.signification ?? "",
      description: parsed.description ?? "",
      jeux_de_mots: parsed.jeux_de_mots ?? "",
      compat_gerard: parsed.compat_gerard ?? "",
      raw: ai,
    };
    await sb.from("analyses").upsert(row, { onConflict: "prenom" });
    return json(row);
  } catch (e) {
    return json({ error: String(e) }, 500);
  }
});

function json(body: unknown, status = 200) {
  return new Response(JSON.stringify(body), {
    status, headers: { ...CORS, "content-type": "application/json" },
  });
}
```

- [ ] **Step 2: Déployer la fonction + secret (utilisateur exécute les commandes Supabase CLI)**

```bash
# Depuis la racine du repo, avec Supabase CLI installé et connecté
supabase link --project-ref wzrcrszfubjsfoaxatvo
supabase secrets set ANTHROPIC_API_KEY=sk-ant-...   # clé fournie par Maxime, JAMAIS commitée
supabase functions deploy analyser-prenom --no-verify-jwt
```
Expected: déploiement OK ; URL `https://wzrcrszfubjsfoaxatvo.supabase.co/functions/v1/analyser-prenom`.

- [ ] **Step 3: Test cURL**

```bash
curl -s -X POST "https://wzrcrszfubjsfoaxatvo.supabase.co/functions/v1/analyser-prenom" \
  -H "content-type: application/json" \
  -d '{"prenom":"Léa","sexe":"f"}'
```
Expected: JSON avec `signification`, `description`, `jeux_de_mots`, `compat_gerard`.

- [ ] **Step 4: Commit**

```bash
git add supabase/functions/analyser-prenom/index.ts
git commit -m "feat: Edge Function analyser-prenom (Claude + cache)"
```

---

## Task 11: Client IA + fiche prénom

**Files:**
- Create: `js/ia.js`
- Create: `js/fiche.js`
- Modify: `js/app.js` (route fiche + init)

- [ ] **Step 1: Créer `js/ia.js`**

```js
import { SUPABASE_URL, SUPABASE_ANON } from "./config.js";

const FN = `${SUPABASE_URL}/functions/v1/analyser-prenom`;

export async function analyserPrenom(prenom, sexe) {
  const res = await fetch(FN, {
    method: "POST",
    headers: { "content-type": "application/json", "authorization": `Bearer ${SUPABASE_ANON}` },
    body: JSON.stringify({ prenom, sexe }),
  });
  const data = await res.json();
  if (data.error) throw new Error(data.error);
  return data;
}
```

- [ ] **Step 2: Créer `js/fiche.js`**

```js
import { analyserPrenom } from "./ia.js";
import { fetchAnalyse } from "./supabase.js";

const el = () => document.getElementById("screen-fiche");
let _current = null; // {prenom, sexe}

export function openFiche(prenom, sexe) { _current = { prenom, sexe }; location.hash = "#fiche"; }

export async function initFiche() {
  if (!_current) { el().innerHTML = `<div class="card">Ouvre une fiche depuis un prénom.</div>`; return; }
  const { prenom, sexe } = _current;
  el().innerHTML = `<div class="card"><h2>${prenom}</h2><p>Chargement de l'analyse…</p></div>`;
  try {
    let a = await fetchAnalyse(prenom);
    if (!a) a = await analyserPrenom(prenom, sexe);
    el().innerHTML = `
      <div class="card">
        <h2>${prenom} <span class="badge-sexe ${sexe||''}">${sexe==='f'?'fille':sexe==='m'?'garçon':''}</span></h2>
        <p><b>Signification :</b> ${a.signification || "—"}</p>
        <p><b>Description :</b> ${a.description || "—"}</p>
        <p><b>Jeux de mots :</b> ${a.jeux_de_mots || "—"}</p>
        <p><b>Avec « Gerard » :</b> ${a.compat_gerard || "—"}</p>
      </div>`;
  } catch (e) {
    el().innerHTML = `<div class="card">⚠️ ${e.message}</div>`;
  }
}
```

- [ ] **Step 3: Câbler dans `js/app.js`**

Ajouter `fiche: initFiche` à `inits`, et un délégué global pour les liens `data-prenom` :

```js
import { initFiche, openFiche } from "./fiche.js";
// ... inits = { jeu: initJeu, fiche: initFiche, ... }

document.addEventListener("click", (e) => {
  const a = e.target.closest("[data-prenom]");
  if (a) { e.preventDefault(); openFiche(a.dataset.prenom, a.dataset.sexe); }
});
```

- [ ] **Step 4: Vérifier bout-en-bout**

Run: `npm run serve` → Jeu → noter un prénom **8+** → « ✨ Analyse… » puis résumé + lien « voir la fiche » → la fiche affiche signification/description/jeux de mots/verdict Gerard.

- [ ] **Step 5: Commit**

```bash
git add js/ia.js js/fiche.js js/app.js
git commit -m "feat: client IA + fiche prénom (analyse Claude)"
```

---

## Task 12: ELO (fonction pure, TDD) + écran Duel

**Files:**
- Create: `js/elo.js`
- Test: `test/elo.test.mjs`
- Create: `js/duel.js`
- Modify: `js/app.js` (init duel)

- [ ] **Step 1: Écrire le test qui échoue**

```js
// test/elo.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeElo } from "../js/elo.js";

test("le gagnant gagne des points, le perdant en perd (égaux au départ)", () => {
  const { winner, loser } = computeElo(1000, 1000, 32);
  assert.equal(winner, 1016);
  assert.equal(loser, 984);
});

test("battre un plus fort rapporte plus", () => {
  const a = computeElo(1000, 1400, 32); // outsider gagne
  assert.ok(a.winner - 1000 > 16);
});
```

- [ ] **Step 2: Lancer → échec**

Run: `node --test test/elo.test.mjs`
Expected: FAIL (module introuvable).

- [ ] **Step 3: Implémenter `js/elo.js`**

```js
export function computeElo(winnerScore, loserScore, k = 32) {
  const ew = 1 / (1 + 10 ** ((loserScore - winnerScore) / 400));
  const el = 1 / (1 + 10 ** ((winnerScore - loserScore) / 400));
  return {
    winner: Math.round(winnerScore + k * (1 - ew)),
    loser: Math.round(loserScore + k * (0 - el)),
  };
}
```

- [ ] **Step 4: Lancer → succès**

Run: `node --test test/elo.test.mjs`
Expected: PASS (2 tests).

- [ ] **Step 5: Créer `js/duel.js`**

```js
import { loadCatalog, pickPair } from "./catalog.js";
import { getParent } from "./profile.js";
import { computeElo } from "./elo.js";
import { fetchElo, saveElo, recordDuel } from "./supabase.js";
import { ELO_DEFAUT, ELO_K } from "./config.js";
import { openFiche } from "./fiche.js";

const el = () => document.getElementById("screen-duel");
let eloMap = null;

function score(p) { return eloMap.get(p) ?? ELO_DEFAUT; }

async function nextPair() {
  const cat = await loadCatalog();
  const [a, b] = pickPair(cat);
  el().innerHTML = `
    <h2>Lequel préfères-tu ?</h2>
    <div style="display:flex; gap:12px">
      ${[a, b].map((p, i) => `
        <div class="card" style="flex:1; text-align:center; cursor:pointer" data-choice="${i}">
          <div class="prenom-nom">${p.prenom}</div>
          <span class="badge-sexe ${p.sexe}">${p.sexe === "f" ? "fille" : "garçon"}</span>
        </div>`).join("")}
    </div>
    <p style="text-align:center"><a href="#" id="skip">Aucun des deux / passer →</a></p>`;

  const pick = async (win, lose) => {
    const r = computeElo(score(win.prenom), score(lose.prenom), ELO_K);
    eloMap.set(win.prenom, r.winner);
    eloMap.set(lose.prenom, r.loser);
    try {
      await saveElo([
        { prenom: win.prenom, score: r.winner },
        { prenom: lose.prenom, score: r.loser },
      ]);
      await recordDuel({ gagnant: win.prenom, perdant: lose.prenom, parent: getParent() });
    } catch (e) { console.warn(e); }
    nextPair();
  };
  el().querySelectorAll("[data-choice]").forEach((c) =>
    c.addEventListener("click", () => c.dataset.choice === "0" ? pick(a, b) : pick(b, a)));
  el().querySelector("#skip").addEventListener("click", (e) => { e.preventDefault(); nextPair(); });
}

export async function initDuel() {
  if (!getParent()) { location.hash = "#accueil"; return; }
  el().innerHTML = `<div class="card">Chargement…</div>`;
  const rows = await fetchElo();
  eloMap = new Map(rows.map(r => [r.prenom, r.score]));
  nextPair();
}
```

- [ ] **Step 6: Câbler `initDuel` dans `js/app.js`**

Ajouter `duel: initDuel` à `inits` (import en tête).

- [ ] **Step 7: Vérifier**

Run: `npm run serve` → onglet Duel → 2 cartes → cliquer un prénom → paire suivante s'affiche ; vérifier dans Supabase que `elo` et `duel_results` se remplissent.

- [ ] **Step 8: Commit**

```bash
git add js/elo.js test/elo.test.mjs js/duel.js js/app.js
git commit -m "feat: ELO (TDD) + écran Duel"
```

---

## Task 13: Dashboard — Top 10 + accord Maman/Papa

Agrège `ratings` côté client : moyenne par prénom, et « coups de cœur communs » = prénoms avec note ≥ SEUIL par maman **et** papa.

**Files:**
- Create: `js/aggregate.js`
- Test: `test/aggregate.test.mjs`
- Create: `js/dashboard.js`
- Modify: `js/app.js` (init dashboard)

- [ ] **Step 1: Écrire le test qui échoue**

```js
// test/aggregate.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { aggregate, coupsDeCoeur } from "../js/aggregate.js";

const rows = [
  { prenom: "Léa", parent: "maman", note: 9, sexe: "f" },
  { prenom: "Léa", parent: "papa", note: 7, sexe: "f" },
  { prenom: "Tom", parent: "maman", note: 8, sexe: "m" },
  { prenom: "Tom", parent: "papa", note: 3, sexe: "m" },
];

test("aggregate calcule la moyenne et trie décroissant", () => {
  const a = aggregate(rows);
  assert.equal(a[0].prenom, "Léa");
  assert.equal(a[0].moyenne, 8);
  assert.equal(a[0].nb, 2);
});

test("coupsDeCoeur = note >= seuil par les deux parents", () => {
  const c = coupsDeCoeur(rows, 7);
  assert.deepEqual(c.map(x => x.prenom), ["Léa"]);
});
```

- [ ] **Step 2: Lancer → échec**

Run: `node --test test/aggregate.test.mjs`
Expected: FAIL (module introuvable).

- [ ] **Step 3: Implémenter `js/aggregate.js`**

```js
export function aggregate(rows) {
  const m = new Map();
  for (const r of rows) {
    if (!m.has(r.prenom)) m.set(r.prenom, { prenom: r.prenom, sexe: r.sexe, notes: [] });
    m.get(r.prenom).notes.push(r.note);
  }
  return [...m.values()]
    .map(x => ({ prenom: x.prenom, sexe: x.sexe, nb: x.notes.length,
      moyenne: Math.round((x.notes.reduce((a, b) => a + b, 0) / x.notes.length) * 10) / 10 }))
    .sort((a, b) => b.moyenne - a.moyenne || b.nb - a.nb);
}

export function coupsDeCoeur(rows, seuil = 7) {
  const byName = new Map();
  for (const r of rows) {
    if (!byName.has(r.prenom)) byName.set(r.prenom, {});
    byName.get(r.prenom)[r.parent] = r.note;
  }
  const out = [];
  for (const [prenom, v] of byName) {
    if (v.maman >= seuil && v.papa >= seuil) {
      const notes = [v.maman, v.papa];
      out.push({ prenom, maman: v.maman, papa: v.papa,
        moyenne: Math.round((notes[0] + notes[1]) / 2 * 10) / 10 });
    }
  }
  return out.sort((a, b) => b.moyenne - a.moyenne);
}
```

- [ ] **Step 4: Lancer → succès**

Run: `node --test test/aggregate.test.mjs`
Expected: PASS (2 tests).

- [ ] **Step 5: Créer `js/dashboard.js`**

```js
import { fetchRatings } from "./supabase.js";
import { aggregate, coupsDeCoeur } from "./aggregate.js";
import { SEUIL_COUP_DE_COEUR } from "./config.js";

const el = () => document.getElementById("screen-dashboard");

export async function initDashboard() {
  el().innerHTML = `<div class="card">Chargement…</div>`;
  const rows = await fetchRatings();
  const top = aggregate(rows).slice(0, 10);
  const coeurs = coupsDeCoeur(rows, SEUIL_COUP_DE_COEUR);
  el().innerHTML = `
    <h2>🏆 Top 10</h2>
    ${top.length ? top.map((t, i) => `
      <div class="card" style="display:flex; justify-content:space-between; align-items:center">
        <span><b>${i + 1}.</b> <a href="#" data-prenom="${t.prenom}" data-sexe="${t.sexe||''}">${t.prenom}</a>
          <span class="badge-sexe ${t.sexe||''}">${t.sexe==='f'?'fille':t.sexe==='m'?'garçon':''}</span></span>
        <b>${t.moyenne}/10</b> <small>(${t.nb} note${t.nb>1?'s':''})</small>
      </div>`).join("") : `<div class="card">Aucune note pour l'instant.</div>`}
    <h2>💞 Coups de cœur communs</h2>
    ${coeurs.length ? coeurs.map(c => `
      <div class="card">
        <b><a href="#" data-prenom="${c.prenom}">${c.prenom}</a></b> — moyenne ${c.moyenne}/10
        <small>(👩 ${c.maman} · 👨 ${c.papa})</small>
      </div>`).join("") : `<div class="card">Pas encore de prénom aimé par les deux (≥ ${SEUIL_COUP_DE_COEUR}).</div>`}`;
}
```

- [ ] **Step 6: Câbler `initDashboard` dans `js/app.js`** (ajouter `dashboard: initDashboard`).

- [ ] **Step 7: Vérifier**

Run: `npm run serve` → noter quelques prénoms en Maman puis en Papa (changer de profil via accueil) → onglet Top → Top 10 trié + section coups de cœur remplie quand ≥7 des deux côtés.

- [ ] **Step 8: Commit**

```bash
git add js/aggregate.js test/aggregate.test.mjs js/dashboard.js js/app.js
git commit -m "feat: dashboard Top 10 + accord Maman/Papa (TDD agrégation)"
```

---

## Task 14: Favoris (❤️) + short-list

**Files:**
- Create: `js/favoris.js`
- Modify: `js/game.js` (bouton cœur sur chaque carte)
- Modify: `js/app.js` (init favoris)

- [ ] **Step 1: Créer `js/favoris.js`**

```js
import { fetchFavoris } from "./supabase.js";
import { getParent } from "./profile.js";

const el = () => document.getElementById("screen-favoris");

export async function initFavoris() {
  el().innerHTML = `<div class="card">Chargement…</div>`;
  const rows = await fetchFavoris();
  const mine = rows.filter(r => r.parent === getParent());
  el().innerHTML = `<h2>❤️ Ma short-list</h2>
    ${mine.length ? mine.map(f => `
      <div class="card"><a href="#" data-prenom="${f.prenom}">${f.prenom}</a></div>`).join("")
      : `<div class="card">Aucun favori. Épingle des prénoms avec le ❤️ pendant le jeu.</div>`}`;
}
```

- [ ] **Step 2: Ajouter le bouton cœur dans `js/game.js`**

Dans `startRound`, ajouter un bouton cœur à chaque carte (après le bloc `.ia-slot`) et l'importer :

```js
import { toggleFavori, fetchFavoris } from "./supabase.js";
```

Dans la construction de `card.innerHTML`, ajouter en haut à droite :
```js
// après avoir créé `card`, insérer un bouton coeur
const heart = document.createElement("button");
heart.className = "btn secondary";
heart.style.cssText = "padding:6px 12px; margin-top:8px";
heart.textContent = "🤍 Favori";
let fav = false;
heart.addEventListener("click", async () => {
  fav = !fav;
  heart.textContent = fav ? "❤️ Favori" : "🤍 Favori";
  try { await toggleFavori(p.prenom, parent, fav); }
  catch (e) { heart.textContent = "⚠️"; console.warn(e); }
});
card.appendChild(heart);
```

- [ ] **Step 3: Câbler `initFavoris` dans `js/app.js`** (ajouter `favoris: initFavoris`).

- [ ] **Step 4: Vérifier**

Run: `npm run serve` → Jeu → cœur sur un prénom → onglet Favoris → il apparaît. Recliquer le cœur le retire (vérifier après rechargement de l'onglet).

- [ ] **Step 5: Commit**

```bash
git add js/favoris.js js/game.js js/app.js
git commit -m "feat: favoris (coeur) + short-list par parent"
```

---

## Task 15: PWA — manifest + service worker + icônes

**Files:**
- Create: `manifest.json`
- Create: `sw.js`
- Create: `icons/icon-192.png`, `icons/icon-512.png`
- Modify: `index.html` (enregistrement SW)

- [ ] **Step 1: Créer `manifest.json`**

```json
{
  "name": "LePrenom",
  "short_name": "LePrenom",
  "description": "Choisir un prénom en s'amusant",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "background_color": "#faf7ff",
  "theme_color": "#7c3aed",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 2: Générer deux icônes PNG (violet + 👶)**

Run (Node, sans dépendance — écrit un PNG violet uni minimal via un canvas OffscreenCanvas n'est pas dispo en Node ; utiliser plutôt un placeholder) :
Créer les icônes avec ImageMagick si disponible :
```bash
magick -size 512x512 xc:'#7c3aed' -gravity center -pointsize 300 -fill white -annotate 0 '👶' icons/icon-512.png
magick icons/icon-512.png -resize 192x192 icons/icon-192.png
```
Si ImageMagick absent : créer `icons/` et y déposer deux PNG carrés (192 et 512) violets. L'appli fonctionne même avec des icônes basiques.
Expected: `icons/icon-192.png` et `icons/icon-512.png` existent.

- [ ] **Step 3: Créer `sw.js` (cache app-shell + catalogue)**

```js
const CACHE = "leprenom-v1";
const ASSETS = [
  "./", "./index.html", "./css/styles.css", "./manifest.json",
  "./data/prenoms.json",
  "./js/app.js", "./js/config.js", "./js/catalog.js", "./js/stars.js",
  "./js/profile.js", "./js/game.js", "./js/duel.js", "./js/elo.js",
  "./js/dashboard.js", "./js/aggregate.js", "./js/favoris.js",
  "./js/fiche.js", "./js/ia.js", "./js/supabase.js",
];

self.addEventListener("install", (e) => {
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)).then(() => self.skipWaiting()));
});
self.addEventListener("activate", (e) => {
  e.waitUntil(caches.keys().then(keys =>
    Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))).then(() => self.clients.claim()));
});
self.addEventListener("fetch", (e) => {
  const url = new URL(e.request.url);
  // Réseau d'abord pour Supabase (API/functions), cache d'abord pour le reste
  if (url.host.includes("supabase.co") || url.host.includes("esm.sh")) return;
  e.respondWith(caches.match(e.request).then(r => r || fetch(e.request)));
});
```

- [ ] **Step 4: Enregistrer le SW dans `index.html`** (avant `</body>`)

```html
<script>
  if ("serviceWorker" in navigator) {
    window.addEventListener("load", () => navigator.serviceWorker.register("sw.js"));
  }
</script>
```

- [ ] **Step 5: Vérifier PWA**

Run: `npm run serve` → DevTools → Application : manifest chargé, SW « activated », icônes visibles. Passer en offline → recharger → l'app-shell et une manche (catalogue en cache) fonctionnent (les écritures échouent hors-ligne, attendu).

- [ ] **Step 6: Commit**

```bash
git add manifest.json sw.js icons/ index.html
git commit -m "feat: PWA (manifest + service worker + icônes)"
```

---

## Task 16: Déploiement GitHub Pages via GitHub Actions

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: Créer `.github/workflows/deploy.yml`**

```yaml
name: Deploy Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: "."
      - id: deployment
        uses: actions/deploy-pages@v4
```

> Note : `data/prenoms.json` est déjà commité (Task 3), donc pas d'étape de build nécessaire dans le workflow. Le site est purement statique.

- [ ] **Step 2: Push initial vers GitHub (token utilisé inline, jamais stocké)**

```bash
git branch -M main
git push "https://checkmydev:ghp_XXXXhttps_TOKENXXXX@github.com/checkmydev/LePrenom.git" main
```
(Remplacer par le token fourni ; ne pas l'écrire dans un fichier.)
Expected: push OK, l'action « Deploy Pages » se lance.

- [ ] **Step 3: Vérifier le déploiement**

Sur GitHub → onglet Actions → workflow vert → l'URL Pages (`https://checkmydev.github.io/LePrenom/`) sert l'appli. Tester le parcours complet en ligne (profil, manche, note >7 → analyse, duel, dashboard, favoris).
Expected: appli fonctionnelle en ligne, installable (PWA).

- [ ] **Step 4: Commit (si ajustements)**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: déploiement GitHub Pages via Actions"
```

---

## Task 17: Vérification finale de bout en bout

- [ ] **Step 1: Lancer toute la suite de tests**

Run: `npm test`
Expected: tous les tests PASS (insee-parse, catalog, elo, aggregate).

- [ ] **Step 2: Checklist manuelle (en ligne)**

- [ ] Accueil : choix Maman/Papa mémorisé.
- [ ] Jeu : GO → 3-2-1-GO! → 10 prénoms mixtes → notation étoiles enregistrée.
- [ ] Note > 7 → analyse IA affichée + fiche complète (signification, description, jeux de mots, verdict Gerard).
- [ ] Duel : choix met à jour l'ELO (vérifier table `elo`).
- [ ] Dashboard : Top 10 correct + coups de cœur communs (tester avec notes ≥7 des deux parents).
- [ ] Favoris : ❤️ ajoute/retire, short-list par parent.
- [ ] PWA : installable, jouable hors-ligne (hors écritures).

- [ ] **Step 3: Commit final éventuel + note de fin**

```bash
git add -A && git commit -m "chore: vérification finale bout-en-bout" || echo "rien à committer"
```

---

## Notes d'implémentation

- **Ordre des dépendances** : `js/ia.js` est importé par `js/game.js` (Task 9) mais créé en Task 11 ; en exécution séquentielle, la note >7 de Task 9 ne pourra être testée qu'après Task 11 — c'est explicité dans la vérification de Task 9. Alternative : créer un stub `js/ia.js` minimal en Task 9. (Le plan assume l'exécution séquentielle et signale le comportement attendu.)
- **Secrets** : `ANTHROPIC_API_KEY` et `service_role` ne sont jamais dans le repo. Seule la clé `anon` (publique) y figure.
- **INSEE** : si l'URL évolue, télécharger manuellement le CSV national et le placer dans `scripts/nat.csv` avant `npm run build:prenoms`.
- **Modèle IA** : `claude-sonnet-5` par défaut (bon rapport qualité/coût). Passer à `claude-opus-4-8` dans l'Edge Function pour une analyse plus poussée.
